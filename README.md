# burn-pytorchreader-bug

This repo contains a reproduction of a bug in `PytorchReader` from the [tracel-ai/burn](https://github.com/tracel-ai/burn) repo. To actually run the code, ensure `uv` is installed on your system. Then, simply use the command `cargo run` to run the code. 
